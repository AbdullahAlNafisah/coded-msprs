__pycache__/
*.pyc
*.nbi
*.nbc
build/
dist/
*.egg-info/
.venv/

# figures/ is generated output, but the rendered figures under it are tracked
# deliberately: the artifact should show its results without being run.
